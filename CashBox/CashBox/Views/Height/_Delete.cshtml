@model CashBoxModel.Height

@using (Ajax.BeginForm("_Delete", "Height", new AjaxOptions()
{
    Confirm = "Are you sure you want to delete this Height?",
    LoadingElementId = "divLoading",
    OnBegin = "DeleteBegin",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "list",
    OnSuccess = "DeleteSuccessful",
    OnFailure = "Failed"

}))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.ID, new { @Id = "txtDeleteID" })

    <input type="submit" id="btnDelete" value="" style="display: none" />
}

